#include "main.h"
/**
 *  *  * _isalpha - checks for alphanumeric character
 *   *   * @c: the character
 *    *    * Return: 1 if the character is lowercase, 0 if not
 */
int _isalpha(int c)
{
if (isalpha(c) == 0)
return (0);
else
return (1);
}
