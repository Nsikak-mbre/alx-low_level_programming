#include "main.h"
/**
 *  *  * swap_int - swaps values
 *   *   * @a: the character
 *   *@b: the second argument
 *    *    * Return: 0
 */
void swap_int(int *a, int *b)
{
if ((*a) && (*b))
{
*a = *b && *b = *a
};
return (0);
}
