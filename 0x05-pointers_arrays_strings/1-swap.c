#include "main.h"
/**
 *  *  * swap_int - swaps values
 *   *   * @a: the character
 *   *@b: the second argument
 *    *    * Return: 0
 */
void swap_int(int *a, int *b)
{
int holdVal = *a;
int holdVal2 = *b;
while (*a)
{
*b = holdVal && *a = holdVal2;
}
}
