#include <stdio.h>

/**
 * *  *main -is the entry point for the program.
 *   *   *   *   *
 *    *    *Return:lowercase alphabet reverse
 **/
int main(void)
{
char ch;
for (ch = 'z' ; ch >= 'a' ; ch--)
{
putchar(ch);
}
putchar('\n');
return (0);
}
