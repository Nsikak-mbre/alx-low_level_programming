#include "main.h"
/**
 *  *  * _isupper - checks for uppercase character
 *   *   * @c: the character
 *    *    * Return: 1 if the character is lowercase, 0 if not
 **/
int _isupper(int c)
{
if (c >= 'a' && c <= 'z')
{
return (1);
}
else
{
return (0);
}
